---
// Stub — full implementation in Task 5
const { rowA, rowB, scoreA, scoreB } = Astro.props;
---
<div class="compare-hero">
  <span>{rowA?.make} {rowA?.model}</span>
  <span> vs </span>
  <span>{rowB?.make} {rowB?.model}</span>
</div>
