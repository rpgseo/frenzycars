name = "frenzycars-blg-webhook"
main = "worker.js"
compatibility_date = "2024-01-01"

[vars]
GITHUB_REPO = "rpgseo/frenzycars"
GITHUB_BRANCH = "main"

# Secrets (set via CLI, NOT here):
#   wrangler secret put BLG_WEBHOOK_SECRET
#   wrangler secret put GITHUB_TOKEN
